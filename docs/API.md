# API
All APIs live under `/api/v1/` and use JWT authentication. Swagger is available at `/api/docs/`.
Each module exposes list, retrieve, create, update, delete, stats, CSV export, filtering, search, ordering, and audit log endpoints.
